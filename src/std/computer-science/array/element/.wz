fun input: [
	array: _
	index: _
] {
	\if index < 0
	\then {
		: fail RangeError
	}

	\if index >= ./size array
	\then {
		: fail RangeError
	}

	\let address: ./base-address array

	: return $ ~/memory/read $ address + index
}
